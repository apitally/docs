---
title: "Reference"
description: "The parameters of our client libraries."
---

<Note>
  Parameters use `snake_case` in our client library for Python and `camelCase`
  in JavaScript.
</Note>

| Parameter                           | Description                                                                                                                                                                                                       | Required | Type      | Default   |
| ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | --------- | --------- |
| `client_id` / `clientId`            | Client ID for your application. Find it on the _Setup instructions_ page for your app.                                                                                                                            | Yes      | `string`  | -         |
| `env`                               | Name of the environment, e.g. `prod` or `dev`. The environment will be automatically created in Apitally.                                                                                                         | No       | `string`  | `default` |
| `sync_api_keys` / `syncApiKeys`     | Whether to enable synchronization of API key hashes from Apitally to your app.                                                                                                                                    | No       | `boolean` | `false`   |
| `openapi_url` / `openApiUrl`        | Path to the API endpoint providing the OpenAPI specification, e.g. `/openapi.json`. Pre-configured for FastAPI.<div style={{ marginTop: "0.5rem" }}>[Learn more](/advanced#capturing-endpoint-descriptions)</div> | No       | `string`  | -         |
| `app_version` / `appVersion`        | The current version of your application, e.g. `1.0.0`.<div style={{ marginTop: "0.5rem" }}>[Learn more](/advanced#reporting-app-versions)</div>                                                                   | No       | `string`  | -         |
| `logger`                            | A custom logger instance. If not provided, a default logger is created automatically.                                                                                                                             | No       | `object`  | -         |
| `key_cache_class` / `keyCacheClass` | A user-provided class for caching API key hashes across app restarts.<div style={{ marginTop: "0.5rem" }}>[Learn more](/advanced#caching-api-key-hashes)</div>                                                    | No       | `type`    | -         |
