<Check>
  You can now create and manage API keys in the Apitally dashboard and they will be
  synchronized to your application. Your application's consumers can authenticate by providing an
  API key in the `Authorization` header with the `ApiKey` scheme prefix.

  Example: `Authorization: ApiKey <api-key>`
</Check>
