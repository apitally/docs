<Warning>
  If you're using uWSGI to serve your {framework} app in production, make sure to run it with the<br />`--enable-threads` and `--lazy-apps` options. Otherwise the Apitally client won't work correctly.
</Warning>
