Here you can also configure the environments (e.g. `prod` and `dev`) for your app, or simply accept the defaults. Note that uptime checks and alerts are automatically enabled for production environments.

Take a note of the client ID displayed after submitting. You will need it in the next step.
